import 'package:flutter/material.dart';

final class AlreadyLoggedView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("You are already logged in :roflanEbalo"));
  }
}